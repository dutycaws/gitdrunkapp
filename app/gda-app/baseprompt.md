# Context
GitDrunkApp is a React app

The goal of the application is to generate a user-friendly webapp that displays bars in a local area and allows the user to rate the bars.

Storage of state is in the client session storage